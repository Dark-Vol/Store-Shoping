import React from "react";
import style from "./Home.module.scss";

const Home: React.FC = () => {
  return (
    <section className={style.wrapper}>
      Home Page
    </section>
  );
};

export default Home;